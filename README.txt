pour compiler : gcc -o (executable) test_ppm_lib.c
pour lancer l'executable : ./(executable)


pour lancer le git:

git add (fichier1) (fichier2) ..
git commit -m "commentaires"
git push


git status (regarder état des fichiers)
